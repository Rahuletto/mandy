/** Default row height for collapsed message rows (must match py-3 + single line content). */
export const VIRTUAL_MESSAGE_ROW_COLLAPSED_PX = 52;

/** Max height of expanded body inside a row (scroll inside). */
export const VIRTUAL_MESSAGE_ROW_BODY_MAX_PX = 300;

export const VIRTUAL_MESSAGE_LIST_OVERSCAN = 8;
